<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://jiadinggai.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://jiadinggai.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-06-17T05:17:00+00:00</updated><id>https://jiadinggai.github.io/feed.xml</id><title type="html">Jiading Gai</title><subtitle>Jiading Gai -- GPU systems, CUDA kernels, compiler engineering, ML infrastructure, and efficient LLM training. </subtitle><entry><title type="html">Transformer MT System: Core Math</title><link href="https://jiadinggai.github.io/blog/2026/mytransformers/" rel="alternate" type="text/html" title="Transformer MT System: Core Math"/><published>2026-06-16T08:00:00+00:00</published><updated>2026-06-16T08:00:00+00:00</updated><id>https://jiadinggai.github.io/blog/2026/mytransformers</id><content type="html" xml:base="https://jiadinggai.github.io/blog/2026/mytransformers/"><![CDATA[<p>Design problem: learn $p(y \mid x)$ for German source tokens $x$ and English target tokens $y$. The implementation, <a href="https://github.com/JiadingGai/exercise/blob/master/NLP/seq2seq/mytransformers.py"><code class="language-plaintext highlighter-rouge">mytransformers.py</code></a>, is adapted from Ben Trevett’s <a href="https://github.com/bentrevett/pytorch-seq2seq/blob/main/legacy/6%20-%20Attention%20is%20All%20You%20Need.ipynb">“6 - Attention is All You Need”</a>.</p> <p>Core model: tokenize <a href="https://github.com/JiadingGai/exercise/tree/master/NLP/seq2seq/data/multi30k"><code class="language-plaintext highlighter-rouge">bentrevett/multi30k</code></a>, embed tokens plus positions, and train an encoder-decoder Transformer.</p> <h2 id="problem-scope">PROBLEM SCOPE</h2> <p><code class="language-plaintext highlighter-rouge">mytransformers.py</code> implements a German-to-English sentence translation system on Multi30k. The scope is offline supervised training and local batch evaluation, not a hosted translation service: inputs are tokenized sentences, outputs are English token sequences, and the main constraints are vocabulary coverage, max decode length, validation loss, perplexity, and BLEU sanity checks.</p> <h2 id="data-pipeline">DATA PIPELINE</h2> <p>The script loads local mirrored <code class="language-plaintext highlighter-rouge">train</code>, <code class="language-plaintext highlighter-rouge">valid</code>, and <code class="language-plaintext highlighter-rouge">test</code> JSONL splits, tokenizes German and English with spaCy, builds source/target vocabularies from the training split, and reserves <code class="language-plaintext highlighter-rouge">&lt;unk&gt;</code>, <code class="language-plaintext highlighter-rouge">&lt;pad&gt;</code>, <code class="language-plaintext highlighter-rouge">&lt;bos&gt;</code>, and <code class="language-plaintext highlighter-rouge">&lt;eos&gt;</code>. <code class="language-plaintext highlighter-rouge">collate_fn</code> numericalizes each pair, adds BOS/EOS, pads variable-length sequences, and feeds padded tensors to <code class="language-plaintext highlighter-rouge">DataLoader</code>.</p> <h2 id="model-core">MODEL CORE</h2> <p>The model core is an encoder-decoder Transformer built from token embeddings, learned positional embeddings, multi-head attention, position-wise feed-forward layers, residual connections, layer normalization, dropout, and source/target masks. The decoder combines causal self-attention with encoder-decoder cross-attention over the encoded German source sequence.</p> <h2 id="embedding-and-masks">EMBEDDING AND MASKS</h2> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Encoder.forward / Decoder.forward
</span><span class="n">h</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">tok_embedding</span><span class="p">(</span><span class="n">tokens</span><span class="p">)</span> <span class="o">*</span> <span class="n">self</span><span class="p">.</span><span class="n">scale</span> <span class="o">+</span> <span class="n">self</span><span class="p">.</span><span class="nf">pos_embedding</span><span class="p">(</span><span class="n">pos</span><span class="p">)</span>
<span class="n">h</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">dropout</span><span class="p">(</span><span class="n">h</span><span class="p">)</span>

<span class="c1"># Seq2Seq masks
</span><span class="n">src_mask</span> <span class="o">=</span> <span class="p">(</span><span class="n">src</span> <span class="o">!=</span> <span class="n">self</span><span class="p">.</span><span class="n">src_pad_idx</span><span class="p">).</span><span class="nf">unsqueeze</span><span class="p">(</span><span class="mi">1</span><span class="p">).</span><span class="nf">unsqueeze</span><span class="p">(</span><span class="mi">2</span><span class="p">)</span>
<span class="n">trg_mask</span> <span class="o">=</span> <span class="n">trg_pad_mask</span> <span class="o">&amp;</span> <span class="n">torch</span><span class="p">.</span><span class="nf">tril</span><span class="p">(</span><span class="n">torch</span><span class="p">.</span><span class="nf">ones</span><span class="p">((</span><span class="n">trg_len</span><span class="p">,</span> <span class="n">trg_len</span><span class="p">),</span> <span class="n">device</span><span class="o">=</span><span class="n">self</span><span class="p">.</span><span class="n">device</span><span class="p">)).</span><span class="nf">bool</span><span class="p">()</span>
</code></pre></div></div> \[h_x^0 = E_x[x]\sqrt{d_{\mathrm{model}}}+P_x,\qquad h_y^0 = E_y[y]\sqrt{d_{\mathrm{model}}}+P_y\] \[M_x(i)=1[x_i \ne \mathrm{pad}],\qquad M_y(i,j)=1[y_i \ne \mathrm{pad}]\,1[j \le i]\] <h2 id="scaled-dot-product-and-multi-head-attention">SCALED DOT-PRODUCT AND MULTI-HEAD ATTENTION</h2> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># MultiHeadAttentionLayer.forward
</span><span class="n">Q</span><span class="p">,</span> <span class="n">K</span><span class="p">,</span> <span class="n">V</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">fc_q</span><span class="p">(</span><span class="n">query</span><span class="p">),</span> <span class="n">self</span><span class="p">.</span><span class="nf">fc_k</span><span class="p">(</span><span class="n">key</span><span class="p">),</span> <span class="n">self</span><span class="p">.</span><span class="nf">fc_v</span><span class="p">(</span><span class="n">value</span><span class="p">)</span>
<span class="n">energy</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">matmul</span><span class="p">(</span><span class="n">Q</span><span class="p">,</span> <span class="n">K</span><span class="p">.</span><span class="nf">permute</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="mi">3</span><span class="p">,</span> <span class="mi">2</span><span class="p">))</span> <span class="o">/</span> <span class="n">self</span><span class="p">.</span><span class="n">scale</span>
<span class="n">energy</span> <span class="o">=</span> <span class="n">energy</span><span class="p">.</span><span class="nf">masked_fill</span><span class="p">(</span><span class="n">mask</span> <span class="o">==</span> <span class="mi">0</span><span class="p">,</span> <span class="o">-</span><span class="mf">1e10</span><span class="p">)</span>
<span class="n">attention</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">softmax</span><span class="p">(</span><span class="n">energy</span><span class="p">,</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>
<span class="n">x</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">matmul</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="nf">dropout</span><span class="p">(</span><span class="n">attention</span><span class="p">),</span> <span class="n">V</span><span class="p">)</span>
</code></pre></div></div> \[\mathrm{Attn}(Q,K,V;M)= \mathrm{softmax}\left(\frac{QK^\top}{\sqrt{d_k}}+M\right)V\] \[\mathrm{MHA}(H_q,H_k,H_v)= \mathrm{Concat}(A_1,\ldots,A_h)W^O,\qquad A_r=\mathrm{Attn}(H_qW_r^Q,H_kW_r^K,H_vW_r^V;M)\] <h2 id="encoder-layer">ENCODER LAYER</h2> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># EncoderLayer.forward
</span><span class="n">_src</span><span class="p">,</span> <span class="n">_</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">self_attention</span><span class="p">(</span><span class="n">src</span><span class="p">,</span> <span class="n">src</span><span class="p">,</span> <span class="n">src</span><span class="p">,</span> <span class="n">src_mask</span><span class="p">)</span>
<span class="n">src</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">self_attn_layer_norm</span><span class="p">(</span><span class="n">src</span> <span class="o">+</span> <span class="n">self</span><span class="p">.</span><span class="nf">dropout</span><span class="p">(</span><span class="n">_src</span><span class="p">))</span>
<span class="n">_src</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">positionwise_feedforward</span><span class="p">(</span><span class="n">src</span><span class="p">)</span>
<span class="n">src</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">ff_layer_norm</span><span class="p">(</span><span class="n">src</span> <span class="o">+</span> <span class="n">self</span><span class="p">.</span><span class="nf">dropout</span><span class="p">(</span><span class="n">_src</span><span class="p">))</span>
</code></pre></div></div> \[\tilde{h}_x^\ell=\mathrm{LN}\left(h_x^{\ell-1}+ \mathrm{MHA}(h_x^{\ell-1},h_x^{\ell-1},h_x^{\ell-1})\right)\] \[h_x^\ell=\mathrm{LN}\left(\tilde{h}_x^\ell+ \mathrm{FFN}(\tilde{h}_x^\ell)\right),\qquad \mathrm{FFN}(z)=\mathrm{ReLU}(zW_1+b_1)W_2+b_2\] <h2 id="encoder-decoder-interaction">ENCODER-DECODER INTERACTION</h2> <p>The encoder output $H_x=h_x^L$ becomes the decoder cross-attention memory.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Seq2Seq.forward
</span><span class="n">enc_src</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">encoder</span><span class="p">(</span><span class="n">src</span><span class="p">,</span> <span class="n">src_mask</span><span class="p">)</span>
<span class="n">output</span><span class="p">,</span> <span class="n">attention</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">decoder</span><span class="p">(</span><span class="n">trg</span><span class="p">,</span> <span class="n">enc_src</span><span class="p">,</span> <span class="n">trg_mask</span><span class="p">,</span> <span class="n">src_mask</span><span class="p">)</span>

<span class="c1"># DecoderLayer.forward
</span><span class="n">_trg</span><span class="p">,</span> <span class="n">_</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">self_attention</span><span class="p">(</span><span class="n">trg</span><span class="p">,</span> <span class="n">trg</span><span class="p">,</span> <span class="n">trg</span><span class="p">,</span> <span class="n">trg_mask</span><span class="p">)</span>
<span class="n">trg</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">self_attn_layer_norm</span><span class="p">(</span><span class="n">trg</span> <span class="o">+</span> <span class="n">self</span><span class="p">.</span><span class="nf">dropout</span><span class="p">(</span><span class="n">_trg</span><span class="p">))</span>
<span class="n">_trg</span><span class="p">,</span> <span class="n">attention</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">encoder_attention</span><span class="p">(</span><span class="n">trg</span><span class="p">,</span> <span class="n">enc_src</span><span class="p">,</span> <span class="n">enc_src</span><span class="p">,</span> <span class="n">src_mask</span><span class="p">)</span>
<span class="n">trg</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">enc_attn_layer_norm</span><span class="p">(</span><span class="n">trg</span> <span class="o">+</span> <span class="n">self</span><span class="p">.</span><span class="nf">dropout</span><span class="p">(</span><span class="n">_trg</span><span class="p">))</span>
</code></pre></div></div> \[\tilde{h}_y^\ell=\mathrm{LN}\left(h_y^{\ell-1}+ \mathrm{MHA}(h_y^{\ell-1},h_y^{\ell-1},h_y^{\ell-1})\right)\] \[\bar{h}_y^\ell=\mathrm{LN}\left(\tilde{h}_y^\ell+ \mathrm{MHA}(\tilde{h}_y^\ell,H_x,H_x)\right)\] \[h_y^\ell=\mathrm{LN}\left(\bar{h}_y^\ell+ \mathrm{FFN}(\bar{h}_y^\ell)\right)\] <h2 id="training-objective">TRAINING OBJECTIVE</h2> <p>Training minimizes teacher-forced token cross-entropy. In <code class="language-plaintext highlighter-rouge">mytransformers.py</code>, the decoder consumes <code class="language-plaintext highlighter-rouge">trg[:, :-1]</code>, predicts the next token sequence <code class="language-plaintext highlighter-rouge">trg[:, 1:]</code>, ignores <code class="language-plaintext highlighter-rouge">&lt;pad&gt;</code> positions in <code class="language-plaintext highlighter-rouge">nn.CrossEntropyLoss</code>, clips gradients, and updates with Adam.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">output</span><span class="p">,</span> <span class="n">_</span> <span class="o">=</span> <span class="nf">model</span><span class="p">(</span><span class="n">src</span><span class="p">,</span> <span class="n">trg</span><span class="p">[:,</span> <span class="p">:</span><span class="o">-</span><span class="mi">1</span><span class="p">])</span>
<span class="n">output</span> <span class="o">=</span> <span class="n">output</span><span class="p">.</span><span class="nf">contiguous</span><span class="p">().</span><span class="nf">view</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="n">output_dim</span><span class="p">)</span>
<span class="n">trg</span> <span class="o">=</span> <span class="n">trg</span><span class="p">[:,</span> <span class="mi">1</span><span class="p">:].</span><span class="nf">contiguous</span><span class="p">().</span><span class="nf">view</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">)</span>
<span class="n">loss</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">CrossEntropyLoss</span><span class="p">(</span><span class="n">ignore_index</span><span class="o">=</span><span class="n">TRG_PAD_IDX</span><span class="p">)(</span><span class="n">output</span><span class="p">,</span> <span class="n">trg</span><span class="p">)</span>
</code></pre></div></div> \[p_\theta(y_t \mid y_{1:t-1},x)= \mathrm{softmax}(h_{y,t}^L W_{\mathrm{vocab}}+b)\] \[\mathcal{L}=-\sum_t \log p_\theta(y_t \mid y_{1:t-1}, x), \qquad p_\theta(y\mid x)=\prod_t p_\theta(y_t \mid y_{1:t-1},x)\] <h2 id="training-system">TRAINING SYSTEM</h2> <p>The implemented training system initializes weights with Xavier uniform, builds padded <code class="language-plaintext highlighter-rouge">DataLoader</code> batches, trains with Adam at <code class="language-plaintext highlighter-rouge">LEARNING_RATE = 0.0005</code>, and uses <code class="language-plaintext highlighter-rouge">nn.CrossEntropyLoss(ignore_index=TRG_PAD_IDX)</code>. Each train step zeroes gradients, runs teacher-forced decoding on <code class="language-plaintext highlighter-rouge">trg[:, :-1]</code>, flattens logits and targets, backpropagates, clips gradients with <code class="language-plaintext highlighter-rouge">CLIP = 1</code>, steps the optimizer, and optionally prints step loss plus running average loss through <code class="language-plaintext highlighter-rouge">LOSS_LOG_EVERY</code>.</p> <p>The epoch loop reads <code class="language-plaintext highlighter-rouge">N_EPOCHS</code> from the environment, evaluates validation loss under <code class="language-plaintext highlighter-rouge">model.eval()</code> and <code class="language-plaintext highlighter-rouge">torch.no_grad()</code>, reports epoch time, train/validation loss, and perplexity, and saves <code class="language-plaintext highlighter-rouge">tut6-model.pt</code> whenever validation loss improves. For qualitative debugging, <code class="language-plaintext highlighter-rouge">SAMPLE_TRANSLATION_EVERY</code> controls printing a fixed German source sentence, the gold English target, and the current greedy translation. After training, the script reloads the best checkpoint and reports test loss, test perplexity, and test BLEU.</p> <h2 id="greedy-decoding">GREEDY DECODING</h2> <p>Inference uses greedy decoding from <code class="language-plaintext highlighter-rouge">&lt;bos&gt;</code> until <code class="language-plaintext highlighter-rouge">&lt;eos&gt;</code>.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># translate_sentence
</span><span class="n">output</span><span class="p">,</span> <span class="n">attention</span> <span class="o">=</span> <span class="n">model</span><span class="p">.</span><span class="nf">decoder</span><span class="p">(</span><span class="n">trg_tensor</span><span class="p">,</span> <span class="n">enc_src</span><span class="p">,</span> <span class="n">trg_mask</span><span class="p">,</span> <span class="n">src_mask</span><span class="p">)</span>
<span class="n">pred_token</span> <span class="o">=</span> <span class="n">output</span><span class="p">.</span><span class="nf">argmax</span><span class="p">(</span><span class="mi">2</span><span class="p">)[:,</span> <span class="o">-</span><span class="mi">1</span><span class="p">].</span><span class="nf">item</span><span class="p">()</span>
<span class="n">trg_indexes</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">pred_token</span><span class="p">)</span>
</code></pre></div></div> \[\hat{y}_t=\arg\max_v p_\theta(v \mid \hat{y}_{1:t-1},x)\] <h2 id="inference-serving">INFERENCE SERVING</h2> <p>The script exposes local inference through <code class="language-plaintext highlighter-rouge">translate_sentence</code>, which tokenizes a sentence, encodes it once, then autoregressively appends the highest-probability target token up to <code class="language-plaintext highlighter-rouge">max_len=50</code>. A served system would wrap the same path with request batching, length limits, input validation, model versioning, and a clear policy for malformed or unsupported-language inputs.</p> <h2 id="bleu-evaluation">BLEU EVALUATION</h2> <p>Validation uses <code class="language-plaintext highlighter-rouge">evaluate</code>, <code class="language-plaintext highlighter-rouge">calculate_bleu</code>, and local <code class="language-plaintext highlighter-rouge">corpus_bleu</code>; BLEU is an unsmoothed sanity check, not a standardized benchmark score.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">test_loss</span> <span class="o">=</span> <span class="nf">evaluate</span><span class="p">(</span><span class="n">model</span><span class="p">,</span> <span class="n">test_dataloader</span><span class="p">,</span> <span class="n">criterion</span><span class="p">)</span>
<span class="n">test_bleu</span> <span class="o">=</span> <span class="nf">calculate_bleu</span><span class="p">(</span><span class="n">test_iter</span><span class="p">[:</span><span class="n">BLEU_EVAL_LIMIT</span><span class="p">],</span> <span class="n">src_vocab</span><span class="p">,</span> <span class="n">trg_vocab</span><span class="p">,</span> <span class="n">model</span><span class="p">,</span> <span class="n">device</span><span class="p">)</span>
</code></pre></div></div> <p>For a dataset $D={(x_i,y_i)}_{i=1}^{m}$, <code class="language-plaintext highlighter-rouge">calculate_bleu</code> greedily decodes candidates $\hat{C}_i$ and uses one reference set $R_i$ per candidate:</p> \[C = \{\hat{C}_i\}_{i=1}^{m},\qquad R = \{R_i\}_{i=1}^{m},\qquad B_D = B(C,R)\] <p>Here $B(C,R)$ is the local <code class="language-plaintext highlighter-rouge">corpus_bleu</code> calculation. For $N=4$ and uniform weights $w_n=\frac{1}{4}$, modified n-gram precision uses clipped counts:</p> \[p_n = \frac{ \sum_i \sum_{g \in G_n(\hat{C}_i)} \min(N(g,\hat{C}_i), \max_{r \in R_i} N(g,r)) }{ \sum_i \max(|\hat{C}_i| - n + 1, 0) }\] <p>With total candidate length $c$ and closest-reference total length $r$, the brevity penalty and BLEU score are:</p> \[BP = \exp(\min(1 - \frac{r}{c}, 0)), \qquad c = \sum_i |\hat{C}_i|\] \[BLEU = BP \cdot \exp(\frac{1}{4}\sum_{n=1}^{4}\log p_n)\] <p>Because the implementation is unsmoothed, it returns <code class="language-plaintext highlighter-rouge">0.0</code> when $c=0$ or when any clipped n-gram count is zero.</p> <h2 id="monitoring-and-failure-modes">MONITORING AND FAILURE MODES</h2> <p>The local script surfaces failures through validation loss, test perplexity, BLEU, and printed sample translations. Typical MT failures to watch are early <code class="language-plaintext highlighter-rouge">&lt;eos&gt;</code>, repeated words, unknown or copied names, wrong-language inputs, long sentences beyond the training distribution, tokenization mismatch, and quality drift when the data domain changes.</p>]]></content><author><name></name></author><category term="notes"/><category term="nlp"/><category term="pytorch"/><category term="research"/><summary type="html"><![CDATA[Core Transformer math for a small German-to-English MT exercise.]]></summary></entry></feed>